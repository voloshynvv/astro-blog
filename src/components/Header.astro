---
import { headerNavigation } from '@/config';

import SiteLogo from '@/components/SiteLogo.astro';

const { pathname } = Astro.url;
---

<header class="fixed left-0 top-0 w-full bg-white">
  <div class="container flex min-h-14 items-center justify-between lg:min-h-20">
    <SiteLogo />

    <nav aria-label="Primary">
      <ul class="flex gap-14">
        {
          headerNavigation.map((navItem) => (
            <li class="inline-flex items-center gap-1">
              <a
                class="transition-colors hover:text-primary aria-current:text-primary"
                href={navItem.href}
                aria-current={pathname === navItem.href ? 'page' : null}
              >
                {navItem.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
</header>
